# Cirkel-flow handoff log

Working companion to `circle-arc-automation-brief.md`. The brief is the static
scope document — this file tracks decisions, unresolved questions, and known
limitations as they land.

## Confirmed workflow

ScriptUI modal dialogs in Photoshop are **application-modal** — they block
all canvas/panel interaction, including pan/zoom and layer-mask toggling.
The flow is therefore designed with **all manual prep done up-front** so the
script never needs to pause for the user mid-detection.

1. Welcome → **Verwerk cirkel…** button.
2. Pick document (existing `pickDocument()` dialog).
3. **Lay-out vraag + mask-prep waarschuwing** (single dialog):
   - "Is de lay-out hetzelfde voor alle maten?"
     - **Hetzelfde** → export only the **largest** catalog Ø (BC 237.5 cm).
       Wallgen scales down for smaller variants.
     - **Verschillend** → ask shape (BC / MS), batch all sizes of that shape.
       MS drops 300 mm; wallgen handles that scale from 100 cm.
   - Warning panel: "Schakel cirkelmaskers nu uit (Shift-klik op de
     mask-thumbnail) voordat je op Volgende klikt. Daarna kan ik niet meer
     pauzeren."
4. If "Verschillend": shape picker (BC / MS).
5. If the master is Duotone, show the Dutch notice and convert the working
   duplicate to Grayscale + `Gray Gamma 1.0`. The master on disk is never
   touched.
6. **Detect circle** on the layered composite (masks now disabled per
   step 3, so detection sees the full unmasked artwork extent).
7. **Confirm + save**: two black rings are drawn on temporary layers
   (`__cirkel_canvas` = detected/canvas edge, `__cirkel_cut` = catalog cut
   line, between them = bleed annulus). The dialog shows the abbreviation
   field, size list, output dir. User saves or cancels. Rings are removed
   after the dialog closes.
8. **Export loop**: flatten merged layers, then for each catalog Ø:
   duplicate working doc, scale detected Ø → (catalog Ø + 2 × bleed), fit
   canvas to that square (centred, white fill if source was smaller),
   assign Gray Gamma 1.0, save TIFF, close duplicate.
9. Working duplicate closes without saving. Master PSD on disk is unmodified.

### Bleed semantics

Bleed lives **inside** the detected circle, not around it:

- Inge's source circle = the **outer extent** of the design (= file canvas).
- Cut line = **catalog Ø**, centred inside the canvas.
- Bleed annulus = the ring between the cut line and the canvas edge that gets
  trimmed off, protecting the cut from white-paper bleed-through.

So a BC 950 export produces a 970 × 970 mm canvas (catalog 950 mm + 2 × 10 mm
bleed), with the detected circle filling the full canvas. The cutter trims
inward to 950 mm, leaving 10 mm of design overdraw on every side.

Output:

- `BC` → 4 TIFFs at 950 / 1425 / 1900 / 2375 mm + 2 × 10 mm bleed each.
- `MS` → 4 TIFFs at 300 / 1000 / 1200 / 1400 mm + 2 × 3 mm bleed each.
- Filename: `{ABBR}_{BC|MS}_{mm4}.tif` (4-digit padded mm).
- Output dir: `{master_dir}/{master_name}_export/`.

## Catalog source-of-truth decision

**Option A — build-time codegen** (brief §3.6).

`scripts/gen-config.py` reads `../../wallgen/var/catalog/dimensions.json` and
emits `src/modules/config-generated.jsx` (gitignored, regenerated by every
build). `config.jsx` retains DPI, bleed, ICC, rectangle/drieluik constants
only — those are tool-internal and not derived from wallgen's catalog.

BC vs MS partitioning is by **explicit key set**, not value threshold:

- BC: `{D_0950, D_1425, D_1900, D_2375}`
- MS: `{D_0300, D_1000, D_1200, D_1400}`

`gen-config.py` fails loud when wallgen adds a new diameter key — better than
silently sliding it into the wrong shape bucket. To add a new size, update both
wallgen's catalog and the key set in `gen-config.py`, then rebuild.

`ARC_W_MM` / `ARC_H_MM` come from the `W_ARC` / `H_ARC` entries in widths/heights.

## Detection contract

**Resolved this session — auto largest-non-white-region only.** No named
mask, no `cirkel` layer convention. Implemented as Action Manager Color Range
sampled on white (luminance 100), inverted to capture every non-white pixel,
read `doc.selection.bounds` as the bounding box, and treat it as the circle's
bbox. Centre = bbox centre, radius = max(half-width, half-height).

### Known limitations

- **Off-centre circles** → bbox centre lies off the design centre.
  `resizeContentToDiameter` still hits the right Ø (it scales the whole doc),
  but the final canvas crop is anchored on the document centre, not the
  design centre — so the cut-line/bleed ring is asymmetric. Mitigation: scope
  is mono masters that are roughly centred. If Inge sees visibly off-centre
  output, log and switch to a layer-mask convention.
- **White-as-design** (logo on a white inner shape) → Color Range can't
  distinguish design-white from background-white. Mitigation: detection runs
  *before* any white background insertion, so source-supplied white still
  reads as background.
- **Stray non-white pixels outside the circle** (artist marks, residual text)
  → inflates the bbox and overshoots the catalog match. Mitigation: confirm
  dialog before export gives Inge a chance to bail.
- **Fully white source** → `doc.selection.bounds` throws; `detectCircle()`
  returns `null`. The flow falls back to a manual size picker.

## Open questions for Inge

Mirrors brief §8.

1. **Canonical BOER variant** — *unresolved*. Validation used
   `BOER_color.psd`. Options remaining: `BOER_cirkel_bron.psd`,
   `BOER_cirkel_bron02.psb`, `BOER_cirkel_bron_DEF.psb`,
   `BOER_cirkel_bron_plat.psd`.
2. **One master per design vs per size** — *unresolved*. Current tool assumes
   one master, all four sizes generated from it. If she prefers per-size
   masters, the batch loop still works (re-imports per size) but the
   abbreviation field will need a stronger override.
3. **Arc batch export** — *unresolved, out of scope this iteration*. Current
   arc support is the single 90×175 cm new-doc preset only. No batch export
   wired.
4. **Circle detection contract** — *resolved this session as auto-only*.
   See "Detection contract" above. Revisit if real masters break detection.

## Validation results

To be filled in after testing on the iMac with at least one of:

- `/tmp/mono-cirkels/WW_color.psb` — circle Ø > canvas. Bleed-after-resize check.
- `/tmp/mono-cirkels/AW_color.psd` — Duotone, large square canvas. Conversion +
  detection check.
- `/tmp/mono-cirkels/BOER_color.psd` — abbreviation extraction
  (`BOER_color.psd` → `BOER`, not `BOER_color`).

### To verify manually

- [ ] Build runs end-to-end and emits `config-generated.jsx`.
- [ ] Welcome dialog shows the third button.
- [ ] Duotone source triggers Dutch conversion notice.
- [ ] Detection picks the visually obvious circle on AW_color.psd.
- [ ] Ambiguous Ø (e.g. WW ~153 cm vs 142.5 / 190 cm) triggers picker.
- [ ] Bleed expands canvas centred + white-filled.
- [ ] 4 TIFFs land in `_export/` subdir with `{ABBR}_BC_{mm4}.tif` names.
- [ ] All TIFFs: Grayscale, Gray Gamma 1.0, LZW, 100 DPI, no alpha, flat.
- [ ] Master file size + mtime unchanged after run.

### ExtendScript surprises encountered

- `executeAction("assignProfile")` is the only path that survived in the
  existing tool — used in `assignGrayGamma()` to satisfy the Gray Gamma 1.0
  contract after `changeMode(GRAYSCALE)`.
- `Color Range` via the action manager needs `colorModel: false` and Lab
  sample values `{L:100, a:0, b:0}` — sampling RGB white at fuzziness 0
  occasionally caught antialias edges, so fuzziness 5 + invert is what made
  detection consistent.
- `doc.selection.bounds` throws if the inversion produced no selection
  (fully white doc). Wrapped in try/catch and surfaced as a manual-size
  fallback rather than a crash.

## Risk register snapshot

| Risk | Status |
|---|---|
| Color Range action codes vary across PS versions | Existing tool already uses action manager (`hasSemiTransparentPixels`) — same surface, low new risk. |
| 500+ MB PSDs slow on flatten/resize | Documented. No progress UI yet. |
| Catalog drift on new wallgen sizes | `gen-config.py` errors on unknown keys; build fails until key set updated. |
